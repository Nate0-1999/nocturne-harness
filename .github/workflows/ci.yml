name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  impact:
    name: Feature impact and handoff evidence
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compute impact and verify DONE handoffs
        env:
          BEFORE: ${{ github.event.before }}
        run: |
          base="$BEFORE"
          if [ "$base" = 0000000000000000000000000000000000000000 ]; then
            base=$(git merge-base origin/main HEAD)
          fi
          python scripts/check_rules.py --freeze-only
          python bin/ledger ci --repo harness "$base..HEAD"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: feature-impact
          path: verification/*/impacted.json

  python:
    name: Python lint and tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checkout lockstep Spine
        uses: actions/checkout@v4
        with:
          repository: Nate0-1999/nocturne-memory
          ref: 9c3f19d6f21a96e4fdbf64c536543fc9e37d134e
          path: _d3/spine
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install harness
        run: |
          python -m pip install --upgrade pip uv
          uv export --frozen --extra dev --no-emit-project --no-emit-package nocturne-memory --no-hashes -o /tmp/harness-ci-constraints.txt
          python -m pip install -c /tmp/harness-ci-constraints.txt ./_d3/spine -e ".[dev]"
      - name: Lint
        run: ruff check . && ruff format --check src tests
      - name: Verify test motivations
        run: python scripts/check_test_motivations.py
      - name: Verify lifecycle matrix
        run: pytest -q tests/test_lifecycle_matrix.py
      - name: Test
        run: pytest -m "not contract"
      - name: Build distributions and rebuild wheel from sdist
        run: |
          python -m pip install build
          python -m build --sdist --wheel
          python scripts/verify_sdist.py dist/nocturne_harness-*.tar.gz
          python -m pip wheel --no-deps --wheel-dir /tmp/nocturne-harness-sdist-wheel dist/nocturne_harness-*.tar.gz
          cmp dist/nocturne_harness-*.whl /tmp/nocturne-harness-sdist-wheel/nocturne_harness-*.whl
      - name: Verify installed rebuilt wheel outside the checkout
        run: |
          python -m venv /tmp/nocturne-wheel
          /tmp/nocturne-wheel/bin/python -m pip install -c /tmp/harness-ci-constraints.txt /tmp/nocturne-harness-sdist-wheel/nocturne_harness-*.whl
          cd /tmp
          /tmp/nocturne-wheel/bin/python "$GITHUB_WORKSPACE/scripts/verify_wheel.py"

  web:
    name: Web lint, build, and UI canon
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Checkout lockstep Spine for the UI canon
        uses: actions/checkout@v4
        with:
          repository: Nate0-1999/nocturne-memory
          ref: 9c3f19d6f21a96e4fdbf64c536543fc9e37d134e
          path: _canon/spine
          persist-credentials: false
      - name: Install web dependencies
        run: npm ci
      - name: Install the UI canon fixture
        working-directory: ${{ github.workspace }}
        run: |
          python -m pip install --upgrade pip uv
          uv export --frozen --extra dev --no-emit-project --no-emit-package nocturne-memory --no-hashes -o /tmp/harness-ci-constraints.txt
          python -m pip install -c /tmp/harness-ci-constraints.txt ./_canon/spine -e ".[dev]"
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build
      - name: Unit tests
        run: npm run test:unit
      - name: Standing rendered UI canon
        # The impact job checks frozen-ledger coverage independently.
        # This job supplies browser regressions, never a packet handoff.
        run: npm run test:canon -- --ui-only
      - name: Verify committed release bundle
        working-directory: .
        run: git diff --exit-code -- web/dist

  contract:
    name: Live Spine contract tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Checkout pinned Spine
        uses: actions/checkout@v4
        with:
          repository: Nate0-1999/nocturne-memory
          ref: 9c3f19d6f21a96e4fdbf64c536543fc9e37d134e
          path: _contract/spine
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install harness
        run: |
          python -m pip install --upgrade pip uv
          uv export --frozen --extra dev --no-emit-project --no-emit-package nocturne-memory --no-hashes -o /tmp/harness-ci-constraints.txt
          python -m pip install -c /tmp/harness-ci-constraints.txt ./_contract/spine -e ".[dev]"
      - name: Run live S1-S2 contract
        env:
          SPINE_SOURCE_DIR: ${{ github.workspace }}/_contract/spine
          PYTHON: python
        run: sh tests/contract/run.sh
