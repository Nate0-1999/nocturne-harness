# SYM10 deliberation verification

These captures prove the fixture-safe acceptance path for SYM10 in the real Rack
shell. Every frame is visibly marked `SYM10 REGRESSION FIXTURE` and
`DETERMINISTIC EVIDENCE · NOT THE OWNER APP`; none is owner-rack evidence.

- `01-deliberation-objective.png` — the explicit phrase stays in the existing
  chat and opens blank, human-authored outcome and motivation fields.
- `02-fixed-charters.png` — the recipe has an explicit search mark and the
  motivation/implementation charters carry fixed rubric and evidence text.
- `03-signed-authority.png` — the performance metric is fixed and the plain T2
  line signs 3 attempts, $10, 3 rounds, depth 2, 4 children, and 30 minutes.
- `04-result-returned.png` — a completed toy Symphony returns inline with its
  distinct stack identity, signed wall, search count, and obvious return copy.

The browser run also read `/v1/symphonies/01M08PF568DSC6DMTHSK3GAA5K` and
confirmed the same source thread, immutable launch artifact, three charter
digests, one marked search step, completed timeline, and signed numeric walls.
No browser warnings or errors were recorded.

All captures are 1650 x 916 PNGs. See `SHA256SUMS` for exact artifact identity
and `SOP.md` for reproduction.
