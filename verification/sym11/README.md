# SYM11 steering evidence

This fixture exercises the three enacted owner-to-conductor intervention classes against a
live, held toy Symphony: clarification, cooperative cancellation, and signed charter fork.
It is deterministic regression evidence, not the owner app and not provider evidence.

The screenshots are accepted only when both `SYM11 REGRESSION FIXTURE` banners are visible.
See `SOP.md` for the exact owner flow and `SHA256SUMS` for capture integrity.
