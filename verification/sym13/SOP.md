# SYM13 owner-style walkthrough

1. Open the isolated Rack and add Recipe from the Stage Library.
2. Make Recipe large enough to read the plan without leaving the Stage.
3. Read from `Packet / input` through `Own prep`, the merged stages, and the
   full-height `Milestone` cell.
4. Confirm passed work is visibly dim but legible, only current SYM13 glows,
   and unstarted cells would remain quiet at full color.
5. Select `Serve the recipe grid`; confirm the inspector repeats its complete
   why and stable identity.
6. Confirm zero browser errors and capture both acceptance states.
