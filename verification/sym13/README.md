# SYM13 plan completion verification

These captures are deterministic fixture evidence, never owner-app or provider
evidence. The fixture is visibly labelled `SYM13 REGRESSION FIXTURE` and runs
away from the owner port.

- `01-symphony-recipe-grid.jpg` — the full Symphony implementation wave as a
  bordered left-to-right recipe: every SYM packet/input has a human one-liner
  and its own prep; dependency streams merge rightward; the milestone is one
  full-height served cell. Actual passed cells are dim and current SYM13 alone
  carries the lit accent.
- `02-current-cell-inspected.jpg` — the selected live SYM13 join and the served
  milestone carry the one current accent. The same browser pass read the
  inspector's complete motivation and stable `SYM13` identity in the DOM.

The fixture uses the same schema-1 graph-history projection and public Rack
query as SYM12. It proves rendering and interaction, not a production Symphony.
