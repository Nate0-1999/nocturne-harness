Build a small standard-library Python CLI named inireport in this empty workspace. Running `python -m inireport INPUT.ini OUTPUT.html` must parse strict INI and atomically write a deterministic UTF-8 HTML audit. Include a doctype and charset, sorted sections and sorted keys, HTML-escaped text, and redaction of values whose key contains password, secret, token, or api_key (case insensitive), showing REDACTED instead. Include summary counts of sections, keys, and redacted values. Disable interpolation so literal percent signs work. Invalid INI, including duplicate keys, must exit nonzero with a clean error and preserve an existing output file. Ship runnable standard-library tests under tests/ and run them. Keep the implementation small and maintainable. Do not inspect other workspaces or prior-round files. You may use memories injected by the Palace. Finish when the program and its tests pass; leave a concise result.
