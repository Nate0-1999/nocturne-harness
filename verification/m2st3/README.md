# M2ST3 — honest display evidence

Deterministic, local-only proof for PLAN M2ST3 findings 7a, 14, and 15. The
fixture deliberately serves long decimal strings, absent gauges, three
data-bearing spend lanes, and ten crowded graph labels. The retained screenshots
show the owner-facing projection; `honest-display.json` records
mechanical collision and width checks plus clean browser error channels.

Run the SOP in `SOP.md` from the Harness repository root.
