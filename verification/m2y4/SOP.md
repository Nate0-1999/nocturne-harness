# M2Y4 builder SOP — 390×844 module reflow

Session: `codex / 2026-08-08 / d4f6`

I evaluated the real owner application, not the deterministic scenario app. I
used the configured Palace and OpenRouter route, and set the browser viewport to
exactly 390×844 CSS pixels.

## Visible pass

1. I expanded Palace Vitals and Context Bars in the rendered rack.
2. I confirmed the shell remained 390/390 and measured the assigned frames:
   Vitals was 226/226 and Context was 163/163 (`clientWidth/scrollWidth`).
3. I inspected each embedded module document. Vitals was 226/226, including
   its lane container; Context was 163/163, including its header. The long
   Vitals gauge rail alone remained a plainly bounded internal scroller.
4. I read the exact `$0.000000160000` value without scrolling the document.

The retained privacy crop is
`01-expanded-modules-390x844.jpg`. It excludes owner memory bodies.

## Populated real-broker pass

1. In a separate, isolated owner home and browser origin, I sent one prompt
   through the real model route `openrouter:minimax/minimax-m3`.
2. I continued through an empty memory gate. It showed zero injected memories
   and zero near misses, and created no memory disposition.
3. I opened Context Bars. It displayed a measured total of 824 input tokens and
   the explicit estimated breakdown: System 145, History 588, Memory 33, and
   Tools 58. Those categories sum to 824. The estimate/80%/compaction disclosure
   remained visible.
4. I opened Vitals. The exact dollar values remained visible; the Vitals root
   and lanes remained 226/226; Context remained 163/163. The browser console
   contained no warnings or errors.

The retained privacy crop is
`02-populated-context-390x844.jpg`. It contains instrument evidence only.

## Judgment and unscripted finding

F024 and F025 are visibly resolved: neither embedded document clips or widens
its assigned rack rectangle, Context preserves measured totals and explicit
estimates, and Vitals preserves exact dollar values while confining its long
rail to an obvious internal scroller.

One independent defect appeared when I selected Vitals CURRENT. The UI entered
its stale state even though the daemon logged two 200 responses for the exact
thread-scoped rack query; selecting GLOBAL immediately cleared the alert. I did
not widen M2Y4 into an already-DONE scope contract. Garden flag F026 records the
separate defect for reconciliation.
