# M2ZC owner-path verification

This packet exercises the current editable owner app with the real daemon,
transcript journal, Rack bridge, and remote Palace read surfaces. It uses an
isolated local home and `m2zc-sop-verification` identity. No prompt, model
request, memory operation, or remote data write occurs.

The final accepted jump creates local thread `f20be5ac…` in project
`m2zc-accept-final`; the Project control renders that value after the daemon
snapshot. The same candidate bundle also reloads thread `2977bd79…` as
`m2zc-build-zcv1`, switches to thread `21c6c214…` as `build-test`, and returns
without resurrecting an old draft.

The refusal proof recreates the historical F035 condition without changing
product data: while the daemon is stopped, the browser creates stale local
thread `dfcf2d14…` requesting `m2zc-refusal-final`; its isolated real journal is
bound to `build-test` before restart. The daemon refuses the conflicting
snapshot with the exact new-thread remedy, and the Project control renders
`build-test`, not the submitted draft.

- `01-refusal-authority.jpg` shows the refused thread after authoritative
  reconciliation: selected thread `DFCF2D14`, Project `build-test`, the exact
  new-thread remedy, and Vitals
  and Context Bars still CURRENT.
- `02-current-scope-coherence.jpg` shows selected thread `F20BE5AC`, Project
  `m2zc-accept-final`, and both visible modules still CURRENT. Server query logs
  bind Vitals and Context Bars to that exact thread ID.
- `trace-summary.json` records the exact acceptance, refusal, reload, switch,
  scope, identity, and zero-provider boundaries.

The screenshot extensions match their JPEG/JFIF bytes. `SHA256SUMS` freezes
the complete packet.
