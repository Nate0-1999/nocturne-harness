# M2XT real-owner micro re-scout

This packet freezes the Card-3 micro re-scout without manufacturing the
learning history it is meant to judge. The run used the real current owner app
and production Palace. It did not use a fixture, send a prompt, call a model,
create a memory, activate a scorer, or change cloud infrastructure.

## Fixed ground

- Harness: `9509f1431b273cde178a8416d3139bcfa315a1fd`
- Spine: `4f81e71d4351d4425f183b6227686a0d7897e253`
- Garden claim: `02b875267cb5dba5bfd3d2ff26489cd34b4e6810`
- Final owner bundle: `index-CeDaoHt_.js`, SHA-256
  `91dc12078c603f523d5849b2a56dd154d2e756da2ee9c57f36533f80e029bf90`
- Isolated identity: principal, machine, and agent
  `m2x-sop-verification`
- Isolated origin: `http://localhost:8799/`

The daemon was launched from the current checkout through
`harness.daemon:create_dev_app` with a private disposable `NOCTURNE_HOME`.
The public owner SPA and its real remote Spine client were used; no scenario or
presentation fixture was mounted.

## Verdicts

| Item | Verdict | Exact observation |
| --- | --- | --- |
| 28 | PASS, carried | Report 090 and `verification/m2z7/trace-summary.json` already prove one real JPEG reached Gemini 2.5 Flash Lite, produced the pixel-dependent 47/Northwest answer, and survived restart. Re-spending adds no evidence. |
| 29 | PASS | Project `m2xt-stage` selected thread `9feb6c0c-9359-4d27-a041-0adda3c5de0d`; project `m2xt-other` selected `2d169d71-b3fc-4c76-b825-88e9242216ab`. Context, Graph, and Injection/Console CURRENT queries changed to the selected UUID; GLOBAL Vitals continued without a thread ID. A round trip back rendered `m2xt-stage`/`9FEB6C0C`, Context stayed CURRENT, and reopened Graph stayed CURRENT. |
| 39 | F036 | The active-live series honestly contains the sole owner event at 0%. There is no measured held-out generation: the Console says `1 legacy generation not recorded`. A second real series cannot be proved before natural owner work yields a measured learner generation. |
| 47 | F036 | Vitals honestly shows 1/25, Right 0, Wrong 1, 0% weighted agreement, and `No measured learning scores yet.` Right/wrong accounting passes; per-generation accuracy cannot yet pass. |
| 48 | PASS | Exactly one FORCE RETRAIN returned `Not enough authentic signals yet: 1 / 25 available.` Receipt `01KZQKRZTXXBBFP778GNR1H4BY` is manual/insufficient_data at one eligible disposition. It created no proposal or activation. |
| 49 | PASS | The canonical read model excludes 93 otherwise-gradable verification/test/fixture dispositions. The floor remains 1/25 and the live series contains only owner event `01KY54AX6YSYMXA4X7V1KZHH2A`. |
| 60 | PASS, carried | Report 093 and `verification/m2zb/trace-summary.json` already prove the real Reka Edge 18,072/16,384 refusal, exact archive/fresh-thread remedy, one request/no retry, and retained 11.8K/16.4K Context Bars through reload. Re-spending adds no evidence. |

F036 is intentionally a natural-evidence constraint, not a request for a code
workaround. Synthetic dispositions, Force values, or an activation would make
the two-series proof less trustworthy rather than more complete.

## Lived path

1. Start on project `m2xt-stage`, keep Vitals GLOBAL, and set Context, Graph,
   and Injection CURRENT. Capture the selected project/thread plus the global
   learning strip (`01-stage-project-vitals.jpg`).
2. Open Injection. Confirm active v0, 1/25 authentic, 24 remaining, 93
   excluded, Right 0, Wrong 1, 0%, no proposal, and no measured generation
   (`02-learning-console-current.jpg`).
3. Press FORCE RETRAIN once. Preserve the exact 1/25 refusal and new
   insufficient-data receipt (`03-retrain-refusal.jpg`). Do not repeat a lost
   response: this append-only receipt is the proof.
4. Close the instrument and use the Project control to select `m2xt-other`.
   Vitals remains GLOBAL while the active thread/project changes
   (`04-other-project-vitals.jpg`). Server request logs show CURRENT
   `context_window`, `memory_graph`, and `scorer_console` queries carrying the
   new thread UUID while GLOBAL `vitals` omits it.
5. Return through the same Project control to `m2xt-stage`, reopen Graph, and
   confirm its CURRENT selection remains pressed (`05-stage-roundtrip-graph-current.jpg`).
6. Check browser errors/warnings (`[]`), close the task tab, stop only the
   exact port-8799 daemon, prove the listener is absent, and remove only the
   validated disposable home.

The unscripted exploration was the close/reopen plus stage→other→stage round
trip. It found no disagreement between the visible Project field, selected
thread, or CURRENT scopes. No hidden button or programmatic state reset is
used as verdict evidence: the Project transitions were submitted with the
owner-visible field and Enter key.

## Residue and boundary

There are no verification memories to tombstone. The five local thread-context
journals were removed with the disposable home after the evidence was frozen.
The only durable remote residue is learner-run receipt
`01KZQKRZTXXBBFP778GNR1H4BY`, created by the one required below-floor retrain.
It is append-only evidence and must not be deleted. The run made zero prompts,
provider requests, scorer activations, credential changes, deployment calls,
or owner-identity writes.

`trace-summary.json` is the machine-readable authority for the exact values and
cross-packet carried evidence. `SHA256SUMS` freezes this directory. A retained
artifact must pass the bounded credential scan before handoff.
