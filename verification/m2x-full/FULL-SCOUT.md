# M2X full pre-gate scout

Session: `codex / 2026-08-08 / e6b1`

Checklist ground: Garden `38feca0`, 58 ratified items

Verdict: **39 PASS / 12 FAIL / 7 NEEDS-TASTE**

This is scout evidence, not owner clearance. The run used the real owner app,
the remote Palace, real OpenRouter chat and embeddings, and the isolated
principal/machine `m2x-sop-verification`. It did not use H5 except for the
deliberate fixture-isolation check in item 54.

`NEEDS-TASTE` is deliberate. Those items require owner authority, authentic
owner dispositions, or work on the owner's real long-running problem. No
verification gesture was substituted for owner evidence.

## Verdict matrix

| # | Verdict | Evidence or finding |
|---:|:---|:---|
| 1 | PASS | `nocturne doctor` reported the remote Palace healthy, journal and disk available, the web app ready, the owner port available, no local toolchain, and schema `0009`. |
| 2 | FAIL | `nocturne deploy --dry-run` produced 19 `NOOP` stages and stage 17 `BLOCKED`: the current Spine source differs while this version is already released. The ratified expectation is 20 `NOOP`. |
| 3 | FAIL | The actual idle deploy was not run because item 2's all-`NOOP` safety precondition failed. The version guard correctly prevented an unsafe immutable-tag rebuild. |
| 4 | PASS | `nocturne up` opened the owner rack, printed the local URL, and became healthy without an update prompt. |
| 5 | PASS | `nocturne open` while running returned successfully without complaint. |
| 6 | PASS | A second `nocturne up --no-open` reported that Nocturne was already running and reused it. |
| 7 | PASS | After Ctrl-C, `nocturne open` returned exactly `Nocturne isn't running — run nocturne up.` with exit 2. |
| 8 | PASS | Reverse-skew prompt inspection found the required plain backup-first explanation and default-deny `y/N` posture. |
| 9 | PASS | Reverse schema skew is refused before serving; the focused regression remained green. |
| 10 | NEEDS-TASTE | Live backup confidence requires the owner's backup and cloud-custody judgment. No backup was enacted. |
| 11 | NEEDS-TASTE | Restore authority belongs to the owner. No restore was requested or enacted. |
| 12 | PASS | Focused reverse-skew and released-image guard checks passed, and the complete baseline remained green. |
| 13 | PASS | The first real turn showed `0 req · 0 in · 0 out` behind the explicit memory gate. After Continue, `openrouter:minimax/minimax-m3` answered using the reviewed memory. See `01-first-turn-gate.jpg`. |
| 14 | PASS | A second message in the same thread had no repeated first-turn modal; contribution revision/weight values were rescored per message. |
| 15 | PASS | `/remember` stored `Moonlit garden verification`, generated a label and keywords, appeared in Memory, and round-tripped `🌙`. |
| 16 | PASS | A rephrased save surfaced `similar memory exists` with the existing ID and update remedy; no duplicate was created. The agent then edited the existing unit. |
| 17 | FAIL | Oversized `/remember` returned `Could not remember: the generated label exceeded 64 characters.` It neither truncated nor performed the required guided semantic split. |
| 18 | PASS | The real agent edit changed body and revision; the revised body injected in a later thread. |
| 19 | PASS | The already-open thread kept its original injected snapshot while a fresh thread gated on the revised body. |
| 20 | PASS | Under the verification identity, keep, irrelevant, wrong, and never produced distinct behaviors. Wrong opened the mandatory correct-or-expire flow. Authentic owner dispositions remain reserved. |
| 21 | PASS | `ORBIT-NINE` was nevered across three threads; the third kill quarantined it, the active count fell from 2 to 1, and it disappeared after refresh. See `02-quarantine-at-three.jpg`. |
| 22 | FAIL | `create_dev_app` supplies `project_key=None`; the owner app exposes no project selector/context, so project-scoped save and cross-project ranking cannot be exercised. |
| 23 | PASS | Multiple empty and near-miss-only cold opens remained graceful and never forced injection. |
| 24 | PASS | Distinctive cobalt/`🌙` content was used in the answer; Memory and Injection showed the exact contributing unit and contribution trace. |
| 25 | PASS | Real archive produced one thread-born candidate in Thread Review; denial drained it and replay remained idempotent. Evidence is retained in `../m2x-rescout/`. |
| 26 | PASS | A real Markdown seed reconciled to one durable batch; same-batch replay did not duplicate it, and UI denial drained the queue. Evidence is retained in `../m2x-rescout/`. |
| 27 | PASS | Thread-born candidates routed to Thread Review/deck while corpus-born candidates routed to the Palace queue. |
| 28 | PASS | The owner chat has no image paste/input surface. The checklist explicitly permits recording absence with its milestone; multimodal input remains an M3 commitment. |
| 29 | FAIL | Context followed the selected thread, but CURRENT Vitals reproduced F026's stale alert over Palace-wide values. The stage therefore cannot turn coherently. See `04-phone-current-vitals.jpg`. |
| 30 | PASS | GLOBAL Graph and GLOBAL Injection both returned real Palace data with no synthetic thread or 503; retained changed-ground evidence covers both. |
| 31 | PASS | Live Context reported 879 tokens, later 912, against the 1M model window; System + History + Memory + Tools equaled the measured total and were labeled estimated. |
| 32 | PASS | The required `390×844` Context module remained within its measured bounds with no horizontal clip. |
| 33 | PASS | GLOBAL Vitals populated lifecycle gauges, category/hour spend lanes, counters, and exact dollars from a real request. |
| 34 | PASS | Vitals reflowed at `390×844`; exact dollar values stayed visible and the long rail remained internally bounded. |
| 35 | PASS | Receipt lanes named purpose/model and exact dollar amounts. In the offline slice, receipt failure did not block chat and the UI disclosed a durable pending receipt count. |
| 36 | PASS | GLOBAL Graph rendered 5 memories/5 relationships with fact/preference/procedure, current, pin, and quarantine encodings. Node selection is wired to the exact Memory editor; the live graph and retained GLOBAL screenshot confirm the real data path. |
| 37 | PASS | Live Memory list, edit, removal/re-add, and pin toggle worked; pin was restored. The enacted scorer law and green regressions confirm pin overflow consumes the regular budget down to zero rather than silently dropping pins. |
| 38 | PASS | Contribution bars summed exactly to `1.00 / 1.00`; active version, provenance, revision, digest, and deep receipt were visible. See `03-injection-deep.jpg`. |
| 39 | FAIL | The owner app renders one DEEP accuracy curve. The live sawtooth plus per-generation two-series score graph required by the checklist is absent. |
| 40 | PASS | DEEP returned 100% over one holdout with digest prefix `313e805f38f7`; changing a knob cleared the receipt and disabled FORCE, and a fresh exact-value DEEP re-armed it. FORCE was not used. |
| 41 | NEEDS-TASTE | The mechanical exact-value interlock passed. Whether to FORCE from a fresh deep result remains the owner's taste/authority; no force occurred. |
| 42 | FAIL | No proposed scorer version exists in the owner app, so the required live challenger audition and separate receipts cannot be observed. The absent `/retrain` path in item 48 prevents reaching this state. |
| 43 | PASS | Model Device showed the resolved route, controls, and history. Temperature changed reversibly to `0.05`, Chat agreed with the active model, and Inherit restored the prior resolution. |
| 44 | PASS | Assistant Markdown rendered and the active model label remained visible and consistent with Chat. |
| 45 | NEEDS-TASTE | The live owner UI showed no packet IDs or relay jargon. One `Garden` phrase remains only in a legacy-fixture cleanup-title set and did not render in the owner run. Boxiness, density, and voice remain the owner's judgment. |
| 46 | PASS | More than ten minutes of unscripted real use produced the recorded failures without detouring into implementation. |
| 47 | FAIL | Vitals exposes no per-generation accuracy series or right/wrong scoreboard. |
| 48 | FAIL | Real `/retrain` passed through the ordinary memory gate and the model answered that it did not know the command; no floor or progress appeared. |
| 49 | FAIL | Spine's verification-hygiene regression is green, but the owner app exposes no retrain/floor surface on which to confirm the provenance count. The ratified owner-facing proof is unavailable. |
| 50 | NEEDS-TASTE | The floor is not met and activation is owner-only. No first retrain or activation was simulated. |
| 51 | FAIL | After Ctrl-C/restart with the same owner home, journal files remained but the active transcript became `Empty` and other threads became `Not loaded`. See `05-restart-persistence-fail.jpg`. |
| 52 | FAIL | The journal grew from 0 to 12 private `0600` files and `doctor` reported it, but startup has no fail-closed writability probe; a non-writable journal is not refused before serving. |
| 53 | PASS | With Spine unreachable, real OpenRouter chat continued, Memory and Vitals disclosed unavailability, `/remember` failed plainly, receipts queued durably, and reconnect cleared the degraded state. |
| 54 | PASS | Deliberate H5 startup used port 8773, displayed `H5 REGRESSION FIXTURE` and `DETERMINISTIC EVIDENCE · NOT THE OWNER APP`, and died with its session. Fixture scorer/Vitals adapter drift emitted diagnostics but did not enter product state. |
| 55 | PASS | Unicode/emoji round-tripped through `/remember`, chat, embedding, and rendering. |
| 56 | PASS | Targeted scans of the retained Markdown and JPEG evidence found no bearer token, key value, or secret. Only plain secret names occurred in command/config inspection. |
| 57 | NEEDS-TASTE | Added by owner commit `38feca0` during this run. It requires the owner's real problem across three threads and two days; verification content cannot prove the founding use. |
| 58 | NEEDS-TASTE | Accumulation quality and honest dispositions must be judged after item 57 on the real project. No synthetic project or scout disposition was substituted. |

## Findings and routing

- F026 remains the known CURRENT Vitals failure and is already owned by M2Y6.
- New mechanical findings are recorded as F027–F031 in Garden: guided split,
  project context, owner-visible learning, restart/journal durability, and the
  released-version dry-run stop.
- Items 42, 47, 48, and 49 are one problem chain rather than four products:
  the owner app has no enacted learning command/surface, so it cannot show the
  signal floor, scoreboard, proposed challenger, or audition.
- Item 28 is not a hidden pass: image input is absent, and M3 is the explicit
  milestone permitted by the checklist.

## Cleanup and exit ground

- The accepted `Moonlit` verification memory was expired through the real
  wrong/expire flow. The final Memory panel showed `0 active units`.
- `ORBIT-NINE` remains only as quarantined verification evidence under the
  verification principal; it cannot inject and is hygiene-excluded.
- Pending seed and thread-review queues were drained.
- The owner, degraded-mode, and H5 processes were stopped. Disposable homes
  and the offline receipt queue were removed. No FORCE, scorer activation,
  backup, restore, deploy, or infrastructure mutation occurred.
- Browser logs were empty before the evidence session closed.
- Harness: `681 passed, 3 deselected`.
- Spine: `223 passed`.
- Web: `10` unit tests, lint, and production build passed.
