# SYM12 Recipe verification

These captures are deterministic fixture evidence, not owner-app evidence. The
fixture is visibly labelled `SYM12 REGRESSION FIXTURE` and runs on port 8872,
never the owner port.

- `01-recipe-graph.jpg` — Graph layer with Memory Graph and Recipe snapped into
  one proximity frame. Recipe shows seven current nodes, six edges, one glowing
  ready node, parallel packet work, a diamond search node, and three gate-shaped
  judges.
- `02-search-selection.jpg` — the ready search selected without leaving the
  Stage; the inspector repeats its complete motivation and stable node identity.

The browser DOM exposed every graph node as a keyboard-focusable button with its
complete label, role, and state. Recipe settings exposed exactly `Everything`
and `This frame`. Browser warnings/errors after the final reload: none.

The server fixture is built from the same schema-versioned history events that
the production read-only projection consumes. It proves rendering and
interaction; it does not claim a live owner Symphony or a production Beads
connection.
