# M3MS real-owner-path proof

This evidence was captured through the real NOCTURNE owner app, not a fixture.
A disposable PostgreSQL/pgvector Palace was migrated through Spine schema
`0017`; the real Harness daemon used the real Spine API, real memory gate, and
OpenRouter (`minimax/minimax-m3`). The proof identity and all memories were
synthetic and packet-scoped.

## What the browser proved

- Injection Console showed the first-class share in the existing scorer
  generation, kept share and tau fixed with 2 of 100 authentic dispositions,
  ran DEEP simulation, and informed-force created a versioned generation at
  1% without locking future learning.
- The first-turn Memory Gate exposed the exact injection and two pinned
  memories before the owner continued.
- Context Bars rendered `Memory block 238 / 10 share (1.0%) · Pinned overflow
  +113` from the observed turn allocation. The 10 unused regular tokens
  remained conversation room; pins crossed the regular ceiling without
  displacing regular memories.
- The OpenRouter-backed response used the injected pinned content and the
  browser console produced no warnings or errors.

## Evidence

- `injection-console.png` — Console share, floor, provenance, and DEEP controls.
- `context-bars-openrouter.png` — completed real-provider turn and honest memory
  block/share/overflow display.
- `trace.json` — redaction-safe identifiers, allocation, learning-floor state,
  provider usage, rendered label, and response captured from the same run.

The disposable services bound only to `127.0.0.1` on ports 18765/18766 and the
database on 55432. No credential is present in these artifacts.
