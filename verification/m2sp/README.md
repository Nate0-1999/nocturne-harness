# M2SP verification evidence

This directory records the deterministic browser proof for Stage polish.

- `01-centered-fine-grid.png` — factory Work composition on the centered fine
  grid, with opposing shell chamfers and the visible `+ Layer` action.
- `02-hard-pan-no-edge.png` — the continuous Stage after a 1,200×600 hard pan;
  no canvas edge is visible and ordinary off-screen recall remains present.
- `03-one-click-layer.png` — `Layer 1` selected immediately after the single
  create action.
- `trace-summary.json` — exact rendered dimensions, grid sizes, pan geometry,
  chamfer shapes, reload result, and browser diagnostics.
- `SOP.md` — first-person use path and experiential verdict.

The fixture is visibly isolated and deterministic. It proves the production UI
behavior and appearance without claiming an owner Palace, provider call, or
real transcript interaction.
