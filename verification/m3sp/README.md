# M3SP live spend-table proof

This packet was charged against a disposable local Palace and the real owner
app, with `openrouter:openai/gpt-5.4` as the chat provider. Nothing here is a
fixture receipt.

`global-attuned-live.png` shows two ordinary Spend instances on the Stage:

- GLOBAL retains both real conversations, the expanded per-model rows, 79
  reasoning tokens, and the non-conversation Embeddings row.
- ATTUNED names `New thread` in its chrome and contains only that active
  conversation.

`live-proof.json` records the exact authoritative projection read through the
authenticated Harness proxy. The Palace and transcript home were disposable;
no owner Palace was read or changed.
