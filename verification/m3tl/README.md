# M3TL real-owner evidence

This directory freezes the disposable real-Palace/OpenRouter walk for “Threads get their own feet.” The screenshots are named by the owner-visible feature they prove. `SOP.md` records the walk and cleanup; `trace-summary.json` records exact machine evidence; `palace_walk.py` is the bounded setup/rank/decision/cleanup helper.

The verification identity was `m3tl-sop-verification`. All folders, prompts, files, memories, injections, and extraction candidates were synthetic. No owner memory was read, changed, or used as proof.

## Gallery

- `01-two-thread-folders.png` — Alpha and Beta were created with independent folder roots.
- `02-wrong-folder-edit-refused.png` — Alpha could not edit a file until it stood in that file's directory.
- `03-move-alpha-beta-unchanged.png` — Alpha moved to `nested`; Beta remained in `beta`.
- `04-fresh-folder-becomes-project.png` — Gamma's typed folder became project label `gamma` and its WHERE.
- `05-same-folder-ranks-over-sibling.png` — the real memory gate shows the Beta-local pair above the Alpha sibling; exact ranks are in `trace-summary.json`.
- `07-compacted-memory-shows-where.png` — approved transcript extraction appears as stored memory with Alpha's `nested` WHERE.
- `08-restart-preserves-thread-locations.png` — after process restart, Alpha is still in `nested`, Beta is still in `beta`, and Gamma retains its folder.
