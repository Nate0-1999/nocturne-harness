# M3TL verification SOP and trace

## Boundary

- Used the real Palace and real OpenRouter through the packaged owner app.
- Disposable identity: `m3tl-sop-verification`.
- Disposable root: `/tmp/nocturne-m3tl-tl02.japUC3`.
- Alpha, Beta, and Gamma folders were synthetic. The only files were `alpha/nested/target.txt` and `beta/beta.txt`.
- The browser was opened manually at `http://localhost:8877/`; no SOP command starts or opens a product browser.

## Walk

1. Started the packaged app from the unrelated `launch` folder and created Alpha and Beta threads by typing their folder paths. Each thread received its own immutable root, basename project label, and initial WHERE.
2. From Alpha's root, asked the real model to edit `nested/target.txt` and stop after the first refusal. The tool returned `Modification requires presence in the file's directory` with the remedy to move into `nested`. The target retained `M3TL original synthetic target.` and Beta retained `M3TL beta thread remains independent.`
3. Moved Alpha through the real model/tool path. The walk exposed and repaired a missing lazy-toolset movement callback plus immediate snapshot publication. Alpha's WHERE became `alpha/nested`; Beta's badge remained `beta`.
4. Created a new thread in the fresh Gamma folder. Its owner-visible project label became `gamma` and its WHERE became the canonical Gamma path.
5. Saved disposable Alpha/Beta memories, then created a controlled global pair with different `origin_location` values. Asked from Beta. Real Palace scorer evidence placed Beta-local `M3TL locality pair indigo` at rank 2, score 0.5918607115745544, WHERE 1.0; Alpha `M3TL locality pair amber` followed at rank 3, score 0.5324112772941589, WHERE 0.0.
6. Archived Alpha's durable transcript through the real extraction service. Approved `Folder-fence rule for edit tool`; the resulting stored procedure memory showed WHERE `/private/tmp/nocturne-m3tl-tl02.japUC3/projects/alpha/nested`.
7. Restarted the packaged process. Alpha restored at `alpha/nested`, Beta at `beta`, and Gamma at `gamma`.
8. Explicitly tombstoned all seven disposable active memories. A final real-Palace read returned zero active memories, zero pending approval-queue items, and zero pending curator cards for the disposable principal.

## Exit gates

- Harness: `1693 passed, 3 deselected in 69.12s`.
- Web: ESLint passed; `132 passed`; production build passed.
- UI canon: packaged heartbeat PASS; 43 rendered states PASS; M2ST1/2/3 PASS; fixture curtain PASS; SYM13 PASS.
- Spine: `298 passed in 21.34s`.
- Focused Ruff and `git diff --check`: PASS. Repository-wide Harness Ruff still contains six unrelated historical import-order findings under older verification packets; no M3TL path has a Ruff finding.

## Cleanup result

`active_memories=0`, `pending_queue_items=0`, `pending_curator_cards=0`. The packaged daemon was stopped and the complete disposable `/tmp/nocturne-m3tl-tl02.japUC3` tree was removed before handoff.
