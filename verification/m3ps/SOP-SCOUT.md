# Pre-review owner scout

This is the frozen findings record for the agent-checkable owner walk against
the released Palace. I used the ordinary packaged Rack, Chrome and the in-app
browser, a disposable local home, a disposable workspace, and the configured
real provider path. Authenticated health reported Palace `0.1.6`, schema
`0017`, and API contract `0.1.6`. No fixture supplied an owner-facing verdict,
no owner memory was written, and no product code was changed.

Verdicts are for aligned behavior only. A FAIL says the observed owner path did
not meet its existing promise; it does not claim a new product rule.

## Standard owner core

### 1 — PASS: the real Rack reaches the released Palace

**WHAT I DID:** Opened the ordinary packaged Rack from the disposable home in
two real browser surfaces and read authenticated health.

**WHAT I EXPECTED:** No fixture curtain; the released Palace becomes ready.

**WHAT I SAW:** The header showed `PALACE READY`; health reported 0.1.6 / 0017 /
0.1.6. The Rack also exposed the separate curator-activity 500 recorded below.
Screenshot: `01-real-owner-rack.png`.

**WHY IT MATTERS:** The walk is on the owner's actual release boundary.

### 2 — FAIL: a first real turn never reaches its gate or provider

**WHAT I DID:** Created fresh ordinary threads, waited for their authoritative
snapshots, and transmitted from both Chrome and the in-app browser. I repeated
the exact test after correcting the daemon root to the disposable workspace.

**WHAT I EXPECTED:** One memory-review gate before any model token, then a real
OpenRouter answer with the model named.

**WHAT I SAW:** The composer retained the prompt and said `The prompt was not
sent. Check the link and try again.` No user message, gate, run, provider call,
or receipt was journaled; the model remained `Choosing model`. Screenshot:
`05-settled-prompt-failure.png`.

**WHY IT MATTERS:** The primary conversation loop cannot begin.

### 3 — FAIL: Unicode remember cannot be exercised

**WHAT I DID:** Reserved a distinctive moon-bearing verification phrase for
the ordinary `/remember` step after the first turn.

**WHAT I EXPECTED:** The command would save the exact Unicode body and expose
it in Memory.

**WHAT I SAW:** The first prompt could not enter the run loop, so issuing a
later `/remember` would not be an executable owner sequence. Memory stayed at
zero active units for this verification principal.

**WHY IT MATTERS:** Storage visibility is not proof if the ordinary owner path
cannot reach it.

### 4 — FAIL: saved memory cannot shape the next answer

**WHAT I DID:** Held the second-turn recall check behind the required ordinary
save.

**WHAT I EXPECTED:** Autonomous scoring without a second modal and a real answer
using the saved phrase.

**WHAT I SAW:** No save or first completion existed, so there was no honest
memory-backed next turn to judge.

**WHY IT MATTERS:** The scout did not substitute direct API state for lived
conversation continuity.

### 5 — FAIL: Graph has no verification unit or provenance to inspect

**WHAT I DID:** Opened the real Graph layer for the verification identity.

**WHAT I EXPECTED:** The remembered unit, relationships, complete text, and
project provenance.

**WHAT I SAW:** Graph honestly showed zero memories and zero relationships
because the ordinary save path was unreachable.

**WHY IT MATTERS:** An empty truthful graph cannot prove memory provenance.

### 6 — FAIL: Injection cannot explain an absent memory-backed turn

**WHAT I DID:** Preserved the Injection check behind the required scored turn.

**WHAT I EXPECTED:** The active recipe and real contribution bars for the
verification memory.

**WHAT I SAW:** No prompt, scoring event, injected unit, or provider answer was
created.

**WHY IT MATTERS:** Explanations must correspond to a real owner turn.

### 7 — FAIL: real provider spend is unavailable

**WHAT I DID:** Opened Spend and retried its ordinary read on released 0.1.6.

**WHAT I EXPECTED:** The real verification turns and model work in exact money
rows without blocking chat.

**WHAT I SAW:** The local read returned 503 and the module said `Detailed spend
needs a newer Palace. Chat is still available.` Chat was independently not
available. Screenshot: `06-spend-needs-newer-palace.png`.

**WHY IT MATTERS:** The review-day release cannot show the promised receipt
truth.

### 8 — FAIL: Context Bars has no measured request or allocation

**WHAT I DID:** Read Context Bars on each selected verification thread.

**WHAT I EXPECTED:** Used capacity plus System, History, Memory, and Tools, with
the real memory-allocation receipt.

**WHAT I SAW:** The module remained `—`; no request or allocation receipt was
created because prompt ingress failed.

**WHY IT MATTERS:** An empty meter is not evidence of request budgeting.

### 9 — FAIL: project binding and sibling isolation cannot be re-proved

**WHAT I DID:** Prepared the disposable workspace and sibling-project sequence,
then stopped it at the failed first turn.

**WHAT I EXPECTED:** Durable project binding and exclusion of the project-local
verification memory from the sibling gate and answer.

**WHAT I SAW:** Only empty `thread_context` records with `build-test` existed;
there was no lawful memory or answer with which to test isolation.

**WHY IT MATTERS:** Empty-thread switching cannot close the earlier lived-data
boundary.

### 10 — FAIL: switching restores only empty threads

**WHAT I DID:** Switched among the browser-created threads and back.

**WHAT I EXPECTED:** The original transcript, memory-backed answer, Memory,
Spend, and Context would return together.

**WHAT I SAW:** Selection and empty thread identities moved coherently, but no
conversation state had been accepted to restore.

**WHY IT MATTERS:** Shell selection is only part of continuity.

### 11 — FAIL: restart restores a catalog, not a lived conversation

**WHAT I DID:** Stopped the real daemon, restarted it against the same
disposable home, reloaded Chrome, and read the thread catalog.

**WHAT I EXPECTED:** Durable transcript, project, memory, and answer state.

**WHAT I SAW:** All empty thread IDs returned, but every journal contained only
one `thread_context` record. There was no accepted transcript to survive.

**WHY IT MATTERS:** Catalog persistence does not prove conversation persistence.

### 12 — PASS: a shipped theme switches, survives reload, and restores

**WHAT I DID:** Selected WIZARD MODE in ordinary settings, reloaded, then
returned to NEO-NOIR.

**WHAT I EXPECTED:** The selected theme would persist and the default would be
recoverable.

**WHAT I SAW:** The shell reported `wizard-mode` before and after reload, then
returned to `neo-noir`.

**WHY IT MATTERS:** Appearance remains a real reversible owner control.

### 13 — PASS: Graph scope is reversible

**WHAT I DID:** Opened Memory Graph settings, changed Everything to Nearest
source, then restored Everything.

**WHAT I EXPECTED:** The visible pressed scope would change without destroying
the layer.

**WHAT I SAW:** Nearest source became pressed, then Everything became pressed
again. Screenshot: `07-graph-scope-restored.png`.

**WHY IT MATTERS:** Ordinary exploration has a visible recovery path.

### 14 — PASS: browser state agrees with durable trace

**WHAT I DID:** Compared browser thread IDs, visible failures, the untouched
probe file, local journals, released health, and local HTTP outcomes.

**WHAT I EXPECTED:** Visible state and durable facts would agree.

**WHAT I SAW:** The corrected-root thread `CC6AB218` had exactly one 172-byte
`thread_context` record, no prompt or run record, and `proof.txt` remained
`before` at SHA-256 `9160d4be34c8695bd172a76c7c7966587ea5a4d991ad22c87b2b91af54aa9ebb`.
Sanitized facts: `trace-summary.json`.

**WHY IT MATTERS:** The first-turn finding is not inferred from a label alone.

### 15 — PASS: cleanup and exit ground are clean

**WHAT I DID:** Confirmed the verification principal wrote zero memories,
closed both browser surfaces, stopped the daemon, removed the disposable home
and workspace, scanned evidence for credential-shaped text, hashed every
screenshot, and reran the full ordinary suites.

**WHAT I EXPECTED:** No owner write, local process, secret, or failing ground
would remain.

**WHAT I SAW:** Zero memory cleanup was required. Exact suite results and hashes
are recorded below and in `SHA256SUMS`.

**WHY IT MATTERS:** Findings remain reproducible without leaving owner state.

## New review-day ground

### 16 — FAIL: proposed replies cannot be tweaked and fired

**WHAT I DID:** The corrected first-turn prompt explicitly requested a short
proposed reply for the next owner action.

**WHAT I EXPECTED:** A visible proposal could be edited and sent as an ordinary
prompt with its source reference preserved.

**WHAT I SAW:** The generating prompt never left the composer, so no proposal
or source reference existed.

**WHY IT MATTERS:** The Deck cannot be judged from component tests alone.

### 17 — FAIL: advance-on-enter has no executable card sequence

**WHAT I DID:** Held the two-card Enter sequence behind real proposed replies.

**WHAT I EXPECTED:** Enter would fire the edited card and immediately advance
focus to the next proposal.

**WHAT I SAW:** No real proposal cards could be produced.

**WHY IT MATTERS:** Focus movement only matters in the lived multi-card path.

### 18 — FAIL: the exact workspace fence and remedy never run

**WHAT I DID:** From the corrected disposable workspace root, submitted an
instruction to edit `src/deep/proof.txt` from the root, follow the named remedy
into `src/deep`, edit `proof.txt`, and read it back.

**WHAT I EXPECTED:** The first write would refuse with the exact fence remedy;
the corrected-location write and read would succeed.

**WHAT I SAW:** The prompt was rejected before any tool call. The file remained
exactly `before` with its original hash.

**WHY IT MATTERS:** A missing tool event cannot be relabeled as a successful
fence refusal.

### 19 — PASS: badges and a real pointer drag re-attune by proximity

**WHAT I DID:** Created multiple Conversation and Context Bars instances on
distinct browser threads, moved a source with a real pointer drag, then dragged
one Context Bars instance from the New thread cluster beside the Restored
conversation source.

**WHAT I EXPECTED:** Stage chrome would name the actual source and change only
after the spatial relationship changed.

**WHAT I SAW:** The consumer badge changed from `New thread` to `Restored
conversation` after the drag; other consumers retained their prior source.
Screenshot: `03-drag-reattunement.png`.

**WHY IT MATTERS:** Placement, not a hidden selected-thread herd, performs the
binding.

### 20 — FAIL: the six-column Spend table is absent on 0.1.6

**WHAT I DID:** Opened GLOBAL Spend on the exact released Palace named by the
packet.

**WHAT I EXPECTED:** Input, KV cache, Reasoning, Output, Total, and Spend per
hour columns over real receipts.

**WHAT I SAW:** Released 0.1.6 returned 404 for the remote table capability;
Harness translated it to a local 503 and the plain newer-Palace fallback.
M3SP's report says the table requires API contract 0.1.9.

**WHY IT MATTERS:** The packet's 0.1.6 ground and its new Spend expectation
cannot both be true today.

### 21 — FAIL: allocation receipts do not appear in Context Bars

**WHAT I DID:** Selected the corrected verification thread and read Context
Bars after the rejected prompt.

**WHAT I EXPECTED:** A real request would expose the allocation receipt beside
the measured context lanes.

**WHAT I SAW:** No request reached prepare, so Context Bars stayed empty and no
allocation receipt existed to render.

**WHY IT MATTERS:** Palace 0.1.6 health alone is not proof that the owner Rack
shows the allocation on a real turn.

## Frozen findings

1. An authoritative fresh thread rejects its first prompt before the memory
   gate, provider, tools, or journal. This blocks thirteen aligned checks.
2. The six-column Spend contract is 0.1.9, while this packet explicitly binds
   the walk to 0.1.6. The shipped fallback is honest, but the required table is
   impossible on that ground.
3. Released 0.1.6 lacks curator activity. Palace State degrades locally to
   `Activity unavailable`, but the same read leaks a repeated 500 as `Rack
   action failed (500)` into Conversation.

No fix, new feature, owner disposition, memory decision, or scorer action was
attempted. The owner-only judgment of calmness, visual taste, and whether the
product is ready for review remains reserved.

## Exit ground

- Spine: `296 passed in 21.10s`.
- Harness: `1688 passed, 3 deselected in 71.89s`.
- Web: `131 passed`; ESLint and production build passed.
- UI canon: fixture curtain, 43-state responsive sweep, Stage, chrome diet,
  honest display, and Recipe canon passed.
- Credential-shaped text scan: no findings.
- Disposable memories: zero written; no tombstone required.
- Disposable home, workspace, browser tabs, and daemon: removed or stopped.
