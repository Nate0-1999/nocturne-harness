# M2K experiential verification

1. Build `web` and launch `PYTHONPATH=src:. uv run --locked python -m verification.run_fixture verification.m2k.scenario_app:create_scenario_app --port 8777`.
2. Open `http://127.0.0.1:8777/?fixture=M2K%20REGRESSION` at 1440×900.
3. Open Graph; verify two differently encoded nodes, similarity edge, inspector, GLOBAL/CURRENT, and node-to-Memory selection.
4. Open Injection; verify exact weighted contribution bars, honest “Not scored yet”, eleven controls, conserved weight sum, and explicit Enact version.
5. Repeat at 390×844 and verify no horizontal overflow.
6. Record screenshots and `/__scenario__/trace`; the fixture banner must remain visible.

This deterministic fixture is product verification, not the real OpenRouter owner app.
