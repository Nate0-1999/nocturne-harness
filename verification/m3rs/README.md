# M3RS comprehensive pre-review walk

Frozen findings-only verification of the released Nocturne owner app against Palace API 0.1.9. Product code was not changed.

## Verdicts

| Step | Feature | Verdict | Frozen observation |
|---:|---|:---:|---|
| 1 | Cold start | FAIL | `nocturne up` reached `Palace ready`; the Rack did not show the version. `nocturne doctor` separately reported API 0.1.9. |
| 2 | A place to work | FAIL | A relative project key worked, but the Rack showed no agent-location badge and project did not become the filesystem folder. |
| 3 | Plan before build | FAIL | Deliberation produced a recipe, judge metrics, and a numeric budget, then ended `Run error · partial kept`; no Symphony card appeared. Plain chat built the peer CLI and both tests passed. |
| 4 | Hands and location rule | FAIL | The agent moved and completed the edit/tests/browser read-back, but the required wrong-folder refusal never appeared. The run also exposed raw process/base64 payloads. |
| 5 | Every module across tabs | FAIL | The modules, two Context Bars, thread badges, badge-flipping drag, Spend, graph, nebula, and Palace State rendered; the failed Symphony left Recipe at zero, so completion-grid dimming was not proven. |
| 6 | Memory loop | FAIL | Remember, panel landing, scored injection/provenance, and removal re-render worked. The gate returned no near misses, so add-back was not exercised. |
| 7 | Isolation and continuity | FAIL | A fresh thread selected zero memories and answered `UNKNOWN`. After restart, thread identities returned as empty/restored stubs instead of reloading exactly as left. |
| 8 | The Deck | FAIL | A prefilled card and alternative rendered; edit, mode toggles, and button-fired countdown worked. Enter did not fire and a multi-card scroll/advance could not be proven. |
| 9 | Steering | FAIL | The Symphony never became a running recipe, so clarification, draining cancel, and charter fork could not be exercised. |
| 10 | Learning machinery | PASS | Force retrain wrote an insufficient-corpus ledger row at 3/25; the active recipe/share stayed fixed below the 100-signal floor. |
| 11 | Curators | FAIL | The real verification identity stayed healthy and clean, but no seeded fixture Palace was available; the owner corpus was not used as a substitute. |
| 12 | Themes and looks | PASS | All five themes rendered, Technomancer persisted across reload, the graph/nebula view rendered, and the idle pair is byte-identical. |
| 13 | Money | PASS | Spend showed build traffic across input, KV cache, reasoning, output, total, and spend/hour; the expanded row split model and embedding costs. |
| 14 | Old core | PASS | Markdown, synthetic image paste/description, Model Device and literal `/model` switch, page and CLI seeds, approve/deny, 390x844 recovery, doctor, and verified cloud backup all worked. |
| 15 | Gallery | PASS | Every numbered feature has a named screenshot, hashes, this verdict ledger, and the Garden report. |

Result: **5 PASS / 10 FAIL**. A FAIL means at least one required clause lacked owner-path proof; aligned sub-behaviors remain recorded above.

## Exit ground

- Spine clean-base distribution: `296 passed in 20.85s`.
- Harness clean-base distribution: `1690 passed, 3 deselected in 69.23s` (only the three remote contract nodes were explicitly deselected).
- Packaged heartbeat and UI canon: PASS, including 43 responsive states and the SYM13 recipe canon.
- Doctor: healthy; remote Palace healthy; API contract 0.1.9.
- Backup: one Cloud SQL backup completed with a verified receipt in the disposable home.
- Cleanup: zero active M3RS memories, zero verification queue cards, zero pending curator cards, no daemon left listening.

## Evidence map

The screenshot filename prefix is the charge step. The plain feature name after it is the owner-facing label. `SHA256SUMS` covers every screenshot and the CLI seed fixture. The detailed chronology is in `SOP.md`; structured outcomes are in `trace-summary.json`.

